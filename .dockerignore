# ============================================================================
# .dockerignore —— 哪些东西**不进** Docker 构建上下文
# ============================================================================
#
# 【为什么需要它？缺了它有什么危险？】
#   ① 镜像体积 / 构建速度：本机 data/ 下有 111MB 真实数据 + 106MB 数据库。
#      .gitignore 忽略了它们，但 **Docker 不看 .gitignore** —— 没有 .dockerignore，
#      这些数据会被全量拷进构建上下文，拖慢 build 甚至撑爆。
#   ② ★ 密钥安全：.env 存着 API Key。一旦进入构建上下文就会被打进镜像，
#      任何人拿到镜像都能读到你的 Key。**明确排除它，是本文件最重要的职责。**
#   ③ 构建可复现：排除本机 data/ 后，镜像内容只取决于仓库里的代码 ——
#      任何人 clone 后 build 得到的都是同一个镜像，而不是「构建者本机恰好有什么」。
#
# 【为什么和 .gitignore 高度重合？】Git 决定「哪些进仓库」，Docker 决定
#   「哪些进镜像」，两者目的不同，但排除对象高度重叠 —— 敏感文件与大体积
#   派生数据两边都不该有。这里有意与 .gitignore 保持一致的排除清单。
# ============================================================================

# ---- 数据与运行时产物（体积大 / 可由脚本重建 / 含外部版权数据）----
data/

# ---- 环境与密钥（绝不能进镜像）----
.env
.env.local
.venv/
venv/

# ---- 版本控制元数据（镜像里不需要）----
.git/
.gitignore
.gitattributes

# ---- Python 缓存与产物 ----
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# ---- 本地个人存档（仅本地保留，不对外）----
*_本地存档.txt

# ---- IDE / 系统 ----
.idea/
.vscode/
.trae/
.DS_Store
Thumbs.db
